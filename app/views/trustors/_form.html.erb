<%= simple_form_for(@trustor) do |f| %>
  <%= f.error_notification %>

  
  <div class="hr-line-dashed"></div>

  <div class="form-group">
      <div class="pull-right m-r-md">
          <%= f.submit t('buttons.save'), class: 'btn btn-primary btn-w-m' %>
      </div>
      <div class="m-l-md">
        <%= link_to t('activerecord.text.back_to_list', model: Trustor.model_name.human), @trustors_path, class: 'btn btn-white' %>
      </div>
  </div>
<% end %>
