<%= simple_form_for(@department) do |f| %>
  <%= f.error_notification %>

  
  <div class="hr-line-dashed"></div>

  <div class="form-group">
      <div class="pull-right m-r-md">
          <%= f.submit t('buttons.save'), class: 'btn btn-primary btn-w-m' %>
      </div>
      <div class="m-l-md">
        <%= link_to t('activerecord.text.back_to_list', model: Department.model_name.human), @departments_path, class: 'btn btn-white' %>
      </div>
  </div>
<% end %>
