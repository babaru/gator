<%= simple_form_for(@product_manager) do |f| %>
  <%= f.error_notification %>

  
  <div class="hr-line-dashed"></div>

  <div class="form-group">
      <div class="pull-right m-r-md">
          <%= f.submit t('buttons.save'), class: 'btn btn-primary btn-w-m' %>
      </div>
      <div class="m-l-md">
        <%= link_to t('activerecord.text.back_to_list', model: ProductManager.model_name.human), product_managers_path, class: 'btn btn-white' %>
      </div>
  </div>
<% end %>
